To install this package, simply run the following command in your project's root
directory.

```shell
yarn add @lucifer1004/react-google-map
```

Or,

```shell
npm install --save @lucifer1004/react-google-map
```
